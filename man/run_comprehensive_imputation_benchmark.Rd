% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imputation.R
\name{run_comprehensive_imputation_benchmark}
\alias{run_comprehensive_imputation_benchmark}
\title{Run comprehensive imputation benchmark}
\usage{
run_comprehensive_imputation_benchmark(
  data,
  target_col,
  feature_cols = NULL,
  id_col = "USUBJID",
  time_col = "Time",
  time_format = "yyyy:mm:dd:hh:nn",
  time_unit = "minute",
  mask_rates = c(0.05, 0.1, 0.2, 0.3, 0.4),
  mask_type = c("random", "block", "gap_block"),
  models = "mice_only",
  rf_n_estimators = 200,
  knn_k = 7,
  xgb_nrounds = 300,
  lgb_nrounds = 400,
  arima_order = c(4L, 1L, 0L),
  seed = 42,
  lag_k = c(1, 2, 3),
  add_rollmean = TRUE,
  roll_window = 3,
  gap_bins = list(c(1, 5), c(6, 35), c(36, NA)),
  gap_probs = c(0.5923, 0.2569, 0.1509),
  open_cap = 0.5
)
}
\arguments{
\item{data}{A data.frame, an object coercible to data.frame, or a path to a
CSV file.}

\item{target_col}{Single character string: target column to mask and impute.}

\item{feature_cols}{Character vector of base feature columns excluding
\code{target_col}. If \code{NULL}, all columns except \code{target_col}, \code{time_col}, and
generated time features are used.}

\item{id_col}{Character string: subject identifier column used for lag
features.}

\item{time_col}{Character string: raw timestamp column to convert into
\code{TimeSeries}.}

\item{time_format}{Character string passed to
\code{CGManalyzer::timeSeqConversion.fn()}.}

\item{time_unit}{Character string passed to
\code{CGManalyzer::timeSeqConversion.fn()}. Use \code{"minute"} or \code{"second"}.}

\item{mask_rates}{Numeric vector in (0, 1): target-row masking rates.}

\item{mask_type}{One of \code{"random"}, \code{"block"}, or \code{"gap_block"}.}

\item{models}{Character vector of models to return. Use \code{"mice_only"},
\code{"rf"}, \code{"knn"}, \code{"xgboost"}, \code{"lightgbm"}, \code{"arima"}, or \code{"all"}.
MICE is always run internally because the other models depend on the
MICE-completed target.}

\item{rf_n_estimators}{Integer: number of random forest trees.
Only used when \code{models} includes \code{"rf"} or \code{"all"}.}

\item{knn_k}{Integer: number of kNN neighbors. Only used when \code{models}
includes \code{"knn"} or \code{"all"}.}

\item{xgb_nrounds}{Integer: number of XGBoost boosting rounds. Only used
when \code{models} includes \code{"xgboost"} or \code{"all"}.}

\item{lgb_nrounds}{Integer: number of LightGBM boosting rounds. Only used
when \code{models} includes \code{"lightgbm"} or \code{"all"}.}

\item{arima_order}{Integer vector of length 3 for \code{forecast::Arima()}. Only
used when \code{models} includes \code{"arima"} or \code{"all"}.}

\item{seed}{Integer seed for masking, MICE, and model reproducibility.}

\item{lag_k}{Integer vector of target lags to compute.}

\item{add_rollmean}{Logical: add rolling mean of prior target values.}

\item{roll_window}{Integer rolling mean window.}

\item{gap_bins}{List of length-2 vectors defining gap-block size bins.}

\item{gap_probs}{Numeric probabilities for \code{gap_bins}.}

\item{open_cap}{Numeric cap used for the open-ended gap bin.}
}
\value{
A list containing \code{results}, a data.frame with columns
\code{MaskRate}, \code{MaskType}, \code{Method}, \code{MAPE}, \code{R2}, \code{MRD}, and \code{MaskedCount};
and \code{imputed_data}, a named list of model-specific completed data.frames.
}
\description{
Benchmarks target imputation under random, contiguous block, or
gap-distribution block masking. The workflow imputes the masked target with
MICE, recomputes lag features from the completed target series, then evaluates
MICE-only, Random Forest, kNN, XGBoost, LightGBM, and ARIMA predictions on the
full target series.
}
\details{
The returned \code{imputed_data} object is a named list with one data.frame per
selected model. Each data.frame stacks rows across mask rates. For unmasked
rows, \code{ImputedValue} equals \code{ObservedValue}; for masked rows, \code{ImputedValue}
is the method-specific imputed or predicted target value.

\code{MRD} follows the comprehensive script convention:
\code{sum(abs(true - pred) / abs(true)) / length(true)}, with zero true values
excluded from the numerator but retained in the denominator. \code{MAPE} is
\code{MRD * 100}. \code{R2} is computed on the same full-length prediction vector.
}
\examples{
data("CGMExampleData")
out <- run_comprehensive_imputation_benchmark(
  CGMExampleData,
  target_col = "LBORRES",
  feature_cols = c("AGE", "hba1c"),
  id_col = "USUBJID",
  time_col = "Time",
  time_format = "yyyy:mm:dd:hh:nn",
  mask_rates = 0.05,
  models = "all",
  rf_n_estimators = 25,
  xgb_nrounds = 25,
  lgb_nrounds = 25
)
out$results
names(out$imputed_data)
head(out$imputed_data$rf)

}
